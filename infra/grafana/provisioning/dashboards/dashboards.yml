apiVersion: 1

providers:
  - name: AeroNet Dashboards
    orgId: 1
    folder: AeroNet OS
    folderUid: aeronet-dashboards
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
